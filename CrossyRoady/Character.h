#pragma once

#include "ConsoleGame.h"

class Character {
   private:
    ConsoleGame::Vec2 coord, size;
    int maxHealth, curHealth, type;
    double speed;
    ConsoleGame::AniSprite anisprite;

   public:
};
